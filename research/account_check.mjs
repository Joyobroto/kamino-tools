// What accounts does our built tx reference? Instrument via a custom script that
// mimics the CLI path but dumps every account + existence check.
// Simpler path: patch CLI temporarily to print transaction message accounts.
console.log("see below");
